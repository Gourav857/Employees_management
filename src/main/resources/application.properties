spring.application.name=Employees_management

# ==========================================
# PRODUCTION SECURE INJECTION WITH DEFAULT FALLBACKS
# ==========================================
spring.datasource.url=jdbc:mysql://3tb91zR7Hgq6jEQ.root:<PASSWORD>@gateway01.ap-southeast-1.prod.alicloud.tidbcloud.com:4000/sys
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Engine Engine
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQL8Dialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Actuator Monitoring Configs
management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always

# Connection Pool Limits
spring.datasource.hikari.maximum-pool-size=10
